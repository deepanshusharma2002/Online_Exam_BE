{
  "name": "node-postgres-prisma",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npx prisma generate",
    "start": "node index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "build": "npm run prisma:generate && npm run prisma:migrate"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "fs": "^0.0.1-security",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.11",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@prisma/client": "^6.18.0",
    "prisma": "^6.18.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  }
}
